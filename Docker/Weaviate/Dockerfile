# ==============================
# 使用 Weaviate 官方镜像
# ==============================
ARG WEAVIATE_VERSION
FROM cr.weaviate.io/semitechnologies/weaviate:${WEAVIATE_VERSION}

# 维护者信息
LABEL maintainer="Plumess"

# 示意暴露服务端口（并未实际映射）
EXPOSE 8080
EXPOSE 50051
