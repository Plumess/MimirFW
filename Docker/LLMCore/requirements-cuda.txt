# PyTorch with CUDA 12.1 (using the specified index URL)
--find-links https://download.pytorch.org/whl/cu121
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# LangChain 额外组件
langchain-huggingface>=0.1,<0.2
langchain-weaviate==0.1.0.dev1
langchain-openai>=0.2,<0.3
# 阿里云模型服务灵积
dashscope

# 境内在线模型下载
modelscope
# huggingface 模型使用框架
transformers
sentence-transformers
# 向量数据库 weaviate 客户端
weaviate-client==4.8.1
# AWQ 量化模型使用库
autoawq==0.2.6

# 推理库（torch 2.3.1 支持该pip版本，可以源码编译新版试试）
vllm==0.5.3.post1

